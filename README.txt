Перед запуском программы необходимо запустить файл проекта init_bd для генерации базы данных (реализация через SQLite). И только после того,
как быстро открылся и закрылся терминал (пересоздался файл shop.bd), необходимо запускать main.py. Должно открыться окно авторизации с выбором режима входа:
(администратор/покупатель)
